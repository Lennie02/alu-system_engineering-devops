# Firewall

This project covers configuring UFW (Uncomplicated Firewall) on web servers.

## Tasks

### 0. Block all incoming traffic but
Configure ufw to block all incoming traffic except:
- Port 22 (SSH)
- Port 80 (HTTP)
- Port 443 (HTTPS SSL)

